# v3.0.3 — Navigation Manager

Admin → Menu & navigation controls visibility and order of pages. Configuration persists in `navigation.json`. Home and Admin cannot be disabled. Profile Sync stays background-only.
