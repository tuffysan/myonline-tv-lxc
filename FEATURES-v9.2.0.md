# v9.2.0 — Unified Playback Engine

Adds a scored playback candidate selector for a common Direct/HLS/Remux/Transcode flow and a matching Android client decision model.

The existing proven live/VOD/series endpoints should be migrated incrementally rather than removed in one step.
