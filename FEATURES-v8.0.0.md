# v8.0.0 — Production Platform
Marks the cumulative server + Web/PWA + Android TV architecture as the v8 production baseline and updates API Contract v1 accordingly. The package contains the hardening/source/playback/device/multi-room/metadata/backup/operations contracts from v7.x. Production status still depends on the explicit v8 release gate and real builds in CI/test LXC.
