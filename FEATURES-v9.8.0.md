# v9.8.0 — Performance & Security

Adds explicit provider/playback resilience defaults and a security/performance verification gate. Runtime middleware and load-test thresholds must be confirmed against the actual deployment before tuning values further.
