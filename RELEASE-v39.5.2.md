# v39.5.2 – Full UI Recovery

- Completes the v39.5.x UI recovery pass.
- Removes experimental One Search overlay and unsupported search endpoint calls.
- Removes duplicate Instant Player overlay and duplicate Live TV remote-key handling.
- Resets experimental v39.5 accessibility state once so recovered screens return to their normal baseline.
- Removes global mobile body safe-area padding and scopes it to action sheets.
- Documents Login → Home → Live → Guide → Movies → Series → Search → My Stuff → Sources → Settings/Admin audit.
- No new features are introduced.
