# v7.7.0 — Backup & Disaster Recovery
Adds versioned backup-manifest and restore-plan contracts plus an explicit protected-data scope. Existing backup readiness remains cumulative. A restore is not considered verified until tested on a disposable LXC.
