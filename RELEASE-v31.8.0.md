# v32.0.0 — Admin & Diagnostics

- Adds a real Admin diagnostics endpoint with uptime, memory, thread count, storage, SQLite counts, FFmpeg, streams, downloads and recent errors.
- Returns 404 to non-admin users.
