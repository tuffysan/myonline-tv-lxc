# v7.8.0 — Admin Operations
Adds a single operations snapshot contract for CPU, memory, disk, active streams, FFmpeg, DVR jobs, online clients, recent errors and version. It is designed to consolidate the existing appliance/health/admin views without exposing provider credentials.
