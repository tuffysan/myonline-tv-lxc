# v8.4.0 — Mobile Remote

Adds server-side trusted-device authorization for remote commands and a common command builder. It is transport-independent so Web/PWA or a future native mobile client can use the same protocol.
