# v38.6.0 – Live Clock

- Updates the application clock automatically every second without requiring a page reload.
- Keeps Live TV clock elements synchronized with the header clock.
- Refreshes the date automatically when the day changes.
- Immediately re-synchronizes visible clocks when returning to the browser tab or app.
