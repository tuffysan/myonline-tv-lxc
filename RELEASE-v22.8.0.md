# v28.0.0 — Search 3.0

This is a cumulative release based on v22.7.0.

Static validation in this build environment covers JavaScript syntax, shell syntax, Kestrel loopback regression and ZIP integrity. The local .NET SDK build is not available here; `PUBLISH.cmd` on Windows/GitHub Actions remains authoritative for C# compilation.
