# v32.0.0 — Unified Library & Search

- Adds SQLite-backed local media search scoped to the signed-in user.
- Adds deterministic preferred-source selection helper for duplicate media.
- Media index table is ready for background catalogue ingestion without cross-user mixing.
