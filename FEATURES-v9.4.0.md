# v9.4.0 — Devices & Pairing

Extends the pairing foundation with persistent-device records, hashed-token storage semantics, revoke state and audit events. Plain device tokens must never be stored server-side.
