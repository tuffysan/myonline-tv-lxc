# v7.5.0 — Real Multi-room
Defines the server realtime envelope and command vocabulary required by SignalR/WebSocket transport for remote control and handoff. This release creates the transport contract; it does not falsely claim a deployed SignalR hub if one is not yet present in the cumulative server.
