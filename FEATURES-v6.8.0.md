# v6.8.0 — Mobile Remote

- Adds the shared native remote-command model: play, pause, next, previous, volume, tune and search.
- This is the client contract needed by a mobile/web remote.
- Network transport is not overclaimed: server-mediated device messaging still needs the paired-device backend.
