public static class Profiles2V2370
{
    public static object Capabilities() => new
    {
        version = "30.0.1",
        feature = "Profiles 2.0",
        localFirst = true,
        mandatoryRuntimeCostSek = 0,
        productionVerified = false,
        description = "Profile, kids-mode and per-profile preference contract."
    };
}
