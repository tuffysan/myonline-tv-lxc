# v6.7.0 — Device & Pairing

- Adds persistent native device identity and friendly device naming.
- Establishes the client identity required for QR pairing, trusted-TV sessions and multi-room.
- QR approval itself requires matching server pairing endpoints and is intentionally not marked complete until that server contract exists.
