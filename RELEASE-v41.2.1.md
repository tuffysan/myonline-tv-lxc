# MyOnline TV v41.2.1 — Subtitle Selection

Adds embedded subtitle discovery and selection for Movies and Series. Supported text subtitle codecs are exposed as authenticated WebVTT tracks. The player shows a CC selector only when usable subtitles exist. Off is the default. Direct Play and HLS video playback pipelines are otherwise unchanged.
