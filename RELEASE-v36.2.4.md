# v36.2.4 – Desktop Hero Artwork & Brand Cleanup

- Desktop/laptop hero artwork now uses `contain` for the media layer so artwork is not excessively zoomed/cropped.
- Hero gradients remain full-size and unused image area blends into the dark surface.
- Sidebar removes the redundant `Web v... · Unified Media Center` line.
- Sidebar brand is now `MyOnlineTV` with the application logo directly after the name on the same row.
- Runtime version remains visible in the main header/status area.
