# v36.5.1 — Mobile Live Channel Fix

Fixes a mobile CSS regression where the generic phone rule hid every `aside` element, including the Live TV channel selector. The rule is now scoped to the application's main navigation sidebar, and the Live channel pane remains visible and scrollable on phones.
