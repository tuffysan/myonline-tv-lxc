# MyOnlineTV v41.0.6 — Continue Watching UX Fix

- Centers the VOD player surface even when an existing `#playerWrap` host is reused.
- Replaces the floating resume strip with a centered Continue Watching modal inside the player stage.
- Dims the video while the resume decision is active.
- Keeps Resume and Start over actions explicit and touch-friendly.
- Uses a compact bottom-sheet presentation on mobile.
- Preserves v41.0.5 fullscreen auto-hide and v41.0.4 playback recovery behavior.
