# v28.1.0 — Admin Backup, Update & Recovery

This is a cumulative Admin release based on v25.6.0.

Static validation in this environment covers JavaScript syntax, shell syntax, Kestrel loopback regression and ZIP integrity. Run `PUBLISH.cmd` to perform the authoritative .NET 10 build before publishing.
